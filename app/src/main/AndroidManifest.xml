<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="justor.prezent2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/koyang_circle_png"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/koyang_circle_png"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Strona_startowa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Wszystkie_powody"/>
        <activity android:name=".Powod_1"/>
        <activity android:name=".Powod_4"/>
    </application>

</manifest>